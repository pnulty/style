# Baseline vs. Syntax Feature Evaluation

Run `python src/experiments/baseline_vs_syntax.py` after the processed feature parquet is available to generate the full results table and plots in `reports/figures/`.
